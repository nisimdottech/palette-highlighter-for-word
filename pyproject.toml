[project]
name = "palette-highlighter-for-word"
version = "1.0.0"
description = "An 81-color highlight palette ribbon for Microsoft Word"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [
    { name = "Nisim Levi", email = "hi@nisim.tech" }
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
